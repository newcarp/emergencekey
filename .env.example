# Primary address shown in URLs (LAN IP or hostname).
HOST=192.168.1.100

# Ports
HTTPS_PORT=8000
HTTP_PORT=8080

# Extra certificate SANs (comma-separated).
# HOST, 127.0.0.1, and localhost are always included.
# Examples:
#   EXTRA_SANS=IP:100.x.x.x,DNS:my-machine.ts.net
EXTRA_SANS=
